import React, { FC, memo } from 'react';

import Taro from '@tarojs/taro';
import { View } from '@tarojs/components';

const Comp: FC = memo((props) => {
  let { children, ...others } = props;
  return <View {...others}>{process.env.TARO_ENV} 暂不支持此组件</View>;
});

export default Comp;
